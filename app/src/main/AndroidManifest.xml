<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laundryclientmobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaundryClientMobile">
        <activity android:name=".ui.extra.PreSelectShopActivity"></activity>
        <activity android:name=".ui.extra.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SelectedShopActivity"
            android:label="@string/title_activity_selected_shop"
            android:theme="@style/Theme.LaundryClientMobile.NoActionBar" />
        <activity android:name=".ui.shop.ShopActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.signup.SignupActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>